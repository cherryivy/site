.eventCard
  = image_tag event.cover_image.url(:medium)
  %br
  %b
    = "#{event.title} by #{event.charity.name}"
    = "(#{event.start_time.strftime("%H:%M")} - #{event.end_time.strftime("%H:%M")})"
  %br
    = event.description
